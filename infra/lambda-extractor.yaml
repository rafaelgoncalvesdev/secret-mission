AWSTemplateFormatVersion: '2010-09-09'
Description: >
  This template defines the Extractor Lambda function, which now acts as a 
  parallel worker to process a segment of the DynamoDB table.

Parameters:
  DynamoDbTableName:
    Type: String
    Description: The name of the DynamoDB table to read from.
  S3BucketName:
    Type: String
    Description: The name of the S3 bucket to write the output file to.

Resources:
  LambdaExtractorRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole

      Policies:
        - PolicyName: LambdaExtractorPermissions
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:Scan
                Resource: !Sub "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${DynamoDbTableName}"

              - Effect: Allow
                Action:
                  - s3:PutObject
                Resource: !Sub "arn:aws:s3:::${S3BucketName}/*"

              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: "arn:aws:logs:*:*:*"

  Extractor:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: db-extractor-lambda
      Runtime: python3.11
      
      # changed: standardized handler name to use an underscore, which is the python module standard.
      Handler: lambda_function.lambda_handler

      Role: !GetAtt LambdaExtractorRole.Arn
      Code:
        S3Bucket: !Ref S3BucketName
        S3Key: "lambda-artifacts/db-extractor.zip"

      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref DynamoDbTableName
          S3_BUCKET_NAME: !Ref S3BucketName
      
      # changed: increased memory to handle larger batch processing.
      MemorySize: 256
      # changed: increased timeout for the longer-running worker task.
      Timeout: 300